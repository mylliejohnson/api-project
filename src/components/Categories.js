import React from 'react';

function Categories(props) {
    return (
        <div>
            this is the categories pages. list all products under that specific category
        </div>
    );
}

export default Categories;